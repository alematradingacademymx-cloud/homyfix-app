# Copia este archivo como .streamlit/secrets.toml para probar localmente
# (ese archivo real NUNCA se sube a GitHub, ya está en .gitignore).
#
# En Streamlit Community Cloud, esto mismo se pega en:
# tu app -> Settings -> Secrets

[homyfix]
api_url = "https://script.google.com/macros/s/TU_ID_DE_DESPLIEGUE/exec"
token = "el-mismo-token-que-pusiste-en-TOKEN_SECRETO-del-Code.gs"
